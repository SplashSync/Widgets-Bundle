{% trans_default_domain "SplashWidgetsBundle" %}   

{% if Widget.Options.EditMode | default(false) %}
    {% set EditAction = "SplashWidgets_LoadEditModal('" ~ Widget.Service ~ "','" ~ Widget.Type ~ "')" %}
    <button class="btn btn-xs btn-primary" onclick="{{ EditAction }}">
        <i class="fa fa-fw fa-gear"></i>
        {{ "widget.params" | trans }}
    </button>
{% endif %}        

    